# Pin the Rust toolchain so `cargo fmt` / clippy give identical results locally
# and in CI -- no version skew in the pre-commit `rust` gate. Bump deliberately.
[toolchain]
channel = "1.97.1"
components = ["clippy", "rustfmt"]
